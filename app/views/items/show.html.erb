<!DOCTYPE html>
<html lang="en">
<head>
 <meta charset="UTF-8">
 <meta name="viewport" content="width=device-width, initial-scale=1.0">
 <title>Stock'Em - <%= @item.item_name %></title>
 <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
 <style>
   body {
     font-family: Arial, sans-serif;
     margin: 0;
     padding: 0;
     background-color: #f8f8f8;
     display: flex;
     flex-direction: column;
     min-height: 100vh;
   }
   header {
     display: flex;
     justify-content: space-between;
     align-items: center;
     background-color: #500000;
     color: white;
     padding: 15px 20px;
   }
   .header-title {
     font-size: 30px;
     font-weight: bold;
   }
   .greeting {
     font-size: 20px;
     margin-right: 10px;
   }
   .user-icon {
     font-size: 20px;
     color: white;
   }
   .main {
     flex: 1;
     padding: 5px 40px;
     background-color: #fff;
     border-radius: 10px;
     box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
     margin: 20px auto;
     max-width: 1200px;
   }
   h1 {
     text-align: center;
     color: #500000;
     margin-bottom: 30px;
   }
   .content-wrapper {
     display: flex;
     justify-content: space-between;
     gap: 40px;
     align-items: flex-start;
     height: 60%;
   }
   @media (max-width: 768px) {
    .content-wrapper {
      flex-direction: column; /* Stack columns on smaller screens */
    }
  }
   .left-column, .right-column {
     flex: 1;
     padding: 20px;
     background-color: #f5f5f5;
     border-radius: 10px;
     box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
     flex-shrink: 0;
     box-sizing: border-box;
   }
   .right-column {
     max-height: 95%; /* Set a maximum height for the right column */
    overflow-y: auto; 
   }
   h2 {
     color: #500000;
     margin-bottom: 15px;
     font-size: 22px;
   }
   p {
     font-size: 16px;
     color: #666;
     margin-bottom: 10px;
   }
   .item-details strong {
     color: #333;
   }
     .item-details{
     padding-left: 2px;}
   .availability {
     font-weight: bold;
   }
   .availability span {
     font-size: 16px;
   }
   img {
     margin: 20px 0;
     max-width: 100%;
     height: auto;
     border-radius: 10px;
     box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
   }
    .btn-container {
      margin-top: 20px;
      display: flex;
      gap: 10px;
    }
    .button-container {
      display: flex;
      flex-direction: column;
      gap: 1rem; /* Adjusts spacing between buttons */
    }
    @media (min-width: 600px) { /* Responsive layout for larger screens */
      .button-container {
        flex-direction: row;
        justify-content: space-evenly; /* Ensures even spacing */
      }
    }
    .action-button {
      flex: 1; /* Allows each button to take equal space */
      padding: 0.5rem 1rem;
    }
   .btn {
     border-style: none;
     padding: 10px 15px;
     background-color: #500000;
     color: white;
     border-radius: 20px;
     text-decoration: none;
     font-weight: bold;
     transition: background-color 0.3s;
   }
   .btn2 {
      border-style: none;
     font-size: 14px;
     padding: 5px 10px;
     background-color: #9c7c08;
     color: white;
     border-radius: 10px;
     font-weight: bold;
     text-decoration: none;
     transition: background-color 0.3s;
   }
   .btn:hover {
     background-color: #800000;
   }
   .event-user {
      font-weight: bold; 
      color: #333; 
      margin: 0; 
      font-size: 0.9em; 
   }
   .event-details {
      color: #555; 
      margin-top: 5px; 
      font-size: 0.8em;
    }


   footer {
     display: flex;
     justify-content: space-between;
     align-items: center;
     background-color: #500000;
     color: white;
     padding: 15px 20px;
     margin-top: auto;
   }

     .editable {
    display: none;
    font-size: 16px;
    color: #333; /* Change to desired color */
    margin-bottom: 10px;
    border: none; /* Remove border if needed */
    outline: none; /* Remove default outline */
    width: calc(100% - 20px); /* Adjust width to avoid offset */
    padding: 5px; /* Add some padding for better appearance */
    margin-left: 0; /* or adjust as necessary */
    padding-left: 0; /* adjust if needed */
  }
  .editable-field {
    margin-left: -15px; /* Reset any margin */
    padding-left: 0; /* Reset any padding */
  }
  .editable strong {
    color: #333; /* Ensure the same color for the editable field */
  }
  .editable-label {
    color: #333; /* Consistent color for strong elements */
  }
  .editable:focus {
      /* outline: none; Remove default outline */
      border-bottom: 1px solid #500000; /* Change bottom border color on focus */
      background-color: white;  
  }
  .editing .editable {
    display: inline; /* Show inputs when editing */
  }
  .editing .display {
    display: none; /* Hide display text when editing */
  }
  .updateForm {
    display: flex;
    justify-content : space-evenly;
    align-items: center;
    margin-left: 0px;
  }
   .modal {
     display: none;
     position: fixed;
     z-index: 1000;
     left: 0;
     top: 0;
     width: 100%;
     height: 100%;
     overflow: auto;
     background-color: rgba(0, 0, 0, 0.4);
   }
   .modal-content {
     background-color: #fff;
     margin: 10% auto;
     padding: 20px;
     border-radius: 10px;
     width: 80%;
     max-width: 600px;
     box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
   }
   .close-btn {
     float: right;
     font-size: 24px;
     font-weight: bold;
     cursor: pointer;
   }
    .editable {
      background-color: #ebebeb;
      border-radius: 7px;
      padding-left: 4px;
      width: 98%
      }
    .editable-label{
    padding-left: 3px;
    }
    .select{
    width: 98%;}
    #item_category, #item_internal_status{
    width: 99.5%;}
    .modal {
  display: none; /* Hidden by default */
  position: fixed; /* Stay in place */
  z-index: 1000; /* On top of other content */
  left: 0;
  top: 0;
  width: 100%;
  height: 100%;
  overflow: auto; /* Enable scroll if needed */
  background-color: rgba(0, 0, 0, 0.5); /* Black background with transparency */
  justify-content: center;
  align-items: center;
}

.modal-content {
  background-color: #fff;
  border-radius: 10px;
  width: 450px;
  padding: 20px;
  text-align: center;
  box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
}

.close-btn {
  font-size: 24px;
  font-weight: bold;
  color: #aaa;
  cursor: pointer;
  position: absolute;
  top: 10px;
  right: 15px;
}

.close-btn:hover {
  color: #000;
}

.modal-buttons {
  margin-top: 20px;
}

.modal-buttons .btn {
  padding: 10px 20px;
  margin: 5px;
  border-radius: 5px;
  cursor: pointer;
  font-size: 16px;
  color: #fff;
  border: none;
}

.modal-buttons .btn:hover {
  opacity: 0.8;
}
 </style>
</head>
<body>


<div class="main">
 <h1><%= @item.item_name %></h1>
  <div class="content-wrapper">
    
    <div class="left-column" style="z-index: 1">
      <h2 style= "padding-left: 4px;">Item Details</h2>
    
      <div class="item-details">
        <p style="padding-left: 2px;"><strong>Item ID:</strong> <%= @item.id %></p>
        <p><strong>Serial Number:</strong> <%= @item.serial_number %></p>
        <p style="padding-left: 2px;"><strong>Item Name:</strong> <%= @item.item_name %></p>
        <p><strong>Category:</strong> <%= @item.category %></p>
        <p><strong>Quality Score:</strong> <%= @item.quality_score %></p>
        <p><strong>Status:</strong> <%= @item.status %></p>
        <p><strong>Details:</strong> <%= @item.details %></p>
      </div>

      <div class="button-container">
        <% if @current_user.auth_level == 1 || @current_user.auth_level == 2 %>
          <% if @item.currently_available %>
            <%= button_to 'Checkout', create_event_path(itemid: @item.id), class: 'btn' %>
          <% else %>
            <%= button_to 'Checkin', create_event_path(itemid: @item.id), class: 'btn' %>
          <% end %> 
        <% end %>
        <% if @current_user.auth_level == 2 %> 
          <button id="edit_button" class='btn' >Edit</button> 
        <% end %>
        <%= button_to 'Back', items_path, method: :get, class: 'btn', form: { class: 'back-button-form'}, id:'back_button' %>
          <% if User.find(session[:user_id]).auth_level == 2 %>
          <%= button_to 'Delete', item_path(@item), method: :delete, form: { data: { turbo_confirm: 'Are you sure?' }}, class: 'btn', id: 'delete_button' %>
          <% end %>
      </div>
    </div>
    
    <div id="editModal" class="modal" style="display: none; position: fixed; z-index: 1000; left: 0; top: 0; width: 100%; height: 100%; background-color: rgba(0, 0, 0, 0.5); justify-content: center; align-items: center;">
      <div class="modal-content" style="background-color: #fff; border-radius: 10px; box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2); max-width: 600px; padding: 20px; box-sizing: border-box; width: 100%; position: relative;">
        <span class="close-btn" style="position: absolute; top: 10px; right: 15px; font-size: 24px; cursor: pointer; color: #333;">&times;</span>
        <h2 style="padding-left: 4px;">Edit Item Details</h2>
        <%= form_with model: @item, local: true do |f| %>
          <div class="item-detail" style="padding-left: 20px; padding-top: 5px;">
            <p class="editable-field">
              <label for="item_name" class="editable-label" style="font-size: 16px; margin-top: 10px; display: flex;"><strong>Item Name:</strong></label>
              <%= f.text_field :item_name, class: "editable", style: "padding: 10px; border: 2px solid #ccc; border-radius: 5px; width: 100%; margin-top: 5px; margin-bottom: 5%; box-sizing: border-box;" %>
            </p>
            
            <% categories = JSON.parse(File.read(Rails.root.join('app/assets/data/category.json')))['data'] %>
            <p class="editable-field">
              <label for="category" class="editable-label" style="font-size: 16px; margin-top: 10px; display: flex;"><strong>Category:</strong></label>
              <%= f.select :category, options_for_select(categories.map { |category| [category, category] }, @item.category), { prompt: 'Select a category' }, { class: "editable", style: "padding: 10px; border: 2px solid #ccc; border-radius: 5px; width: 100%; margin-top: 5px; margin-bottom: 5%; box-sizing: border-box;" } %>
            </p>
            
            <p class="editable-field">
              <label for="quality_score" class="editable-label" style="font-size: 16px; margin-top: 10px; display: flex;"><strong>Quality Score:</strong></label>
              <%= f.text_field :quality_score, class: "editable", style: "padding: 10px; border: 2px solid #ccc; border-radius: 5px; width: 100%; margin-top: 5px; margin-bottom: 5%; box-sizing: border-box;" %>
            </p>
            
            <p class="editable-field">
              <label for="status" class="editable-label" style="font-size: 16px; margin-top: 10px; display: flex;"><strong>Status:</strong></label>
              <%= f.select :status, options_for_select([nil, 'Available', 'Damaged', 'Lost', 'Not Available'], @item.status), {}, { class: "editable", id: 'item_internal_status', style: "padding: 10px; border: 2px solid #ccc; border-radius: 5px; width: 100%; margin-top: 5px; margin-bottom: 5%; box-sizing: border-box;" } %>
            </p>
            
            <p class="editable-field">
              <label for="details" class="editable-label" style="font-size: 16px; margin-top: 10px; display: flex;"><strong>Details:</strong></label>
              <%= f.text_area :details, class: "editable", style: "padding: 10px; border: 2px solid #ccc; border-radius: 5px; width: 100%; margin-top: 5px; margin-bottom: 5%; box-sizing: border-box; resize: none;" %>
            </p>
          </div>
          
          <div class="btn-container" style="display: flex; justify-content: center; margin-top: 20px;">
            <button type="submit" id="submit_button" class="btn" style="background-color: #500000; color: white; border: none; border-radius: 5px; padding: 10px 15px; font-size: 16px; cursor: pointer; transition: background-color 0.3s; margin-right: 10px;">Save</button>
            <button type="button" id="cancel_button" class="btn" style="background-color: #500000; color: white; border: none; border-radius: 5px; padding: 10px 15px; font-size: 16px; cursor: pointer; transition: background-color 0.3s;">Cancel</button>
          </div>
        <% end %>
      </div>
    </div>
    <div class="right-column">
        <h2>Item History</h2>
        <div class="notes-section" style="padding-right: 10px; ">
        <% @events.each do |event| %>
          <br>
          <p class="event-user">
            <%= User.find(event.associated_user_id).first_name %> 
            <%= User.find(event.associated_user_id).last_name %> 
            (<%= event.created_at.to_s.split[0] %>):
          </p>
          <p class="event-details">
            <%= event.details %>
          </p>
        <% end %>
      </div>
      </div>
    <div class="right-column">
    <div style="display: flex; flex-direction: row; align-content: stretch; align-items: center">
      <h2>Notes</h2>

      <!-- Add in "create note" button if not writing a note already -->
      <% if (User.find(session[:user_id]).auth_level == 2 || User.find(session[:user_id]).auth_level == 1) && !@writing_note %>
        <%= link_to 'Create Note', item_path(id: @item.id, writing_note: true), class: 'btn2',  style: 'margin-bottom: 5px; margin-left: 30px;' %>
      <% end %>
      </div>

      <!-- Note creation form -->
      <% if @writing_note %>
        <%= form_tag(add_note_path, method: "get") do %>
          <%= label_tag(:note_msg, '') %>
          <%= text_area_tag(:note_msg) %>
          <%= hidden_field_tag :id, @item.id %>
          <br>
          <input type="submit" value="Publish" class="btn2">
        <% end %>
      <% end %>

      <!-- Scrollable Notes Section -->
      <%# <div class="notes-section" style="max-height: 210px; padding-right: 10px; "> %>
        <% @notes.each do |note| %>
          <br>
          <p class="event-user">
            <%= User.find(note.user_id).first_name %> 
            <%= User.find(note.user_id).last_name %> 
            (<%= note.created_at.to_s.split[0] %>):
          </p>
          <p class="event-details">
            <%= note.msg %>
          </p>
        <% end %>
    </div>
  </div>
  <% if User.find(session[:user_id]).auth_level != 0 %>
    <div style="background-color: #f5f5f5; border-radius: 10px; box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1); padding: 20px;">
      <h3 style="color:#500000">Update:</h3>
      <div style="display:flex; justify-content:flex-start; align-items: center;">
        <%= form_with model: @item, url: set_status_item_path(@item), class:"updateForm", method: :patch, local: true do |f| %>
          <label for="status"><strong>Select Status:</strong></label>
          <%= f.select :status, options_for_select(
            [['Clear Status', nil], 'Damaged', 'Lost', 'Not Available', 'Intact'].reject { |s| s[1] == @item.status }, # Exclude the current status
            @item.status # Pre-select the current status if it’s not nil
          ),  {}, { style: "margin-left: 18px; padding: 10px; border-radius: 5px; border: 2px solid #ccc; background-color: #f5f5f5;", name: "item[status]" } %>
          
          <div style="margin-left: 10px;">
            <%= f.submit "Update Status", class: "btn" %>
          </div>
        <% end %>
      </div>
    </div>
  <% end %>

</div>
<div id="deleteModal" class="modal">
  <div class="modal-content">
    <h2>Are you sure you want to delete this item?</h2>
    <div class="modal-buttons">
      <button id="confirmDelete" class="btn" style="background-color: #500000;">Yes</button>
      <button id="cancelDelete" class="btn" style="background-color: #500000;">No</button>
    </div>
  </div>
</div>
<script>
var deleteModal = document.getElementById('deleteModal');
var deleteButton = document.getElementById('delete_button');
var closeModal = document.querySelector('.close-btn');
var confirmDelete = document.getElementById('confirmDelete');
var cancelDelete = document.getElementById('cancelDelete');

// When the user clicks the "Delete" button, show the modal
deleteButton.addEventListener('click', function(event) {
  event.preventDefault(); // Prevent default form submission
  
  // Show the modal
  deleteModal.style.display = 'flex';
});

// When the user clicks the "X" (close) button, close the modal
closeModal.addEventListener('click', function() {
  deleteModal.style.display = 'none';
});

// When the user clicks "Cancel", close the modal
cancelDelete.addEventListener('click', function() {
  deleteModal.style.display = 'none';
});

// When the user clicks "Yes, Delete", submit the delete form
confirmDelete.addEventListener('click', function() {
  deleteModal.style.display = 'none'; // Close the modal
  // Programmatically submit the form to delete the item
  deleteButton.closest('form').submit();
});

// When the user clicks anywhere outside the modal, close it
window.addEventListener('click', function(event) {
  if (event.target == deleteModal) {
    deleteModal.style.display = 'none';
  }
});
  setTimeout(function() {
    var flashNotice = document.getElementById('flash-notice');
    if (flashNotice) {
      flashNotice.style.display = 'none'; 
    }
  }, 4000); 

  document.getElementById('edit_button').addEventListener('click', function() {
    // Show editable fields and hide display fields
    document.querySelectorAll('.editable').forEach(function(input) {
        input.style.display = 'inline'; // Show input fields
        input.parentElement.classList.add('editing'); // Add editing class
    });
    
    // Show the dropdowns specifically for category and status
    document.querySelectorAll('select.editable').forEach(function(select) {
        select.style.display = 'inline'; // Show dropdowns
    });
    
    document.querySelectorAll('.display').forEach(function(span) {
        span.style.display = 'none'; // Hide display text
    });
    document.getElementById('action_buttons').style.display = 'block'; // Show action buttons
    this.style.display = 'none'; // Hide the edit button
    });

    document.getElementById('cancel_button').addEventListener('click', function() {
    // Hide editable fields and show display fields
    document.querySelectorAll('.editable').forEach(function(input) {
        input.style.display = 'none'; // Hide input fields
    });

    // Hide the dropdowns specifically for category and status
    document.querySelectorAll('select.editable').forEach(function(select) {
        select.style.display = 'none'; // Hide dropdowns
    });
    
    document.querySelectorAll('.display').forEach(function(span) {
        span.style.display = 'inline'; // Show display text
    });
    document.getElementById('action_buttons').style.display = 'none'; // Hide action buttons
    document.getElementById('edit_button').style.display = 'inline'; // Show the edit button again
    });
      document.getElementById('edit_button').addEventListener('click', function() {
    document.getElementById('editModal').style.display = 'block';
  });

  document.querySelector('.close-btn').addEventListener('click', function() {
    document.getElementById('editModal').style.display = 'none';
  });
  document.getElementById('cancel_button').addEventListener('click', function() {
    document.getElementById('editModal').style.display = 'none';
  });
  window.addEventListener('click', function(event) {
    if (event.target == document.getElementById('editModal')) {
      document.getElementById('editModal').style.display = 'none';
    }
  });
</script>
</body>
</html>